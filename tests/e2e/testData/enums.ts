export enum IbfStyles {
  noTrigger = 'no-alert',
  trigger = 'alert',
}

export enum UserRole {
  Admin = 'admin',
  LocalAdmin = 'local-admin',
  Operator = 'operator',
  Pipeline = 'pipeline',
  Viewer = 'viewer',
}
