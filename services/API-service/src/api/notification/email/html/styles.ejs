<style>
  /* Body text styles */
  .body-text {
    color: black;
    font-size: 18px;
    font-family: Arial, Helvetica, sans-serif;
    line-height: 22.4px;
    word-wrap: break-word;
  }
  .body-text-bold {
    font-weight: 700;
  }
  .body-text-normal {
    font-weight: 400;
  }
  .body-text-smaller {
    font-size: 16px;
    line-height: 19.2px;
  }
  .body-text-normal-stretch {
    align-self: stretch;
    text-align: left;
    color: black;
  }
  .body-text-white {
    color: white;
  }

  /* Icon styles */
  .body-text-icon {
    width: 14px;
    height: 14px;
  }

  /* Text formatting styles */
  .body-text-break {
    display: block;
  }
  .text-centered {
    text-align: center;
  }
  .text-underlined {
    text-decoration: underline;
  }

  /* Table styles */
  .table-row {
    width: 423px;
    padding-top: 4px;
    padding-bottom: 4px;
    border: 1px #f5f6f9 solid;
  }
  .header-row {
    justify-content: flex-start;
    align-items: center;
    gap: 40px;
    display: inline-flex;
  }
  .table-cell {
    height: 19px;
    padding: 2px 10px;
    width: 211px;
  }

  /* Event table styles */
  .event-table {
    width: 100%;
    height: 100%;
    padding: 10px;
    background: white;
    justify-content: center;
    align-items: center;
    gap: 10px;
  }

  /* Miscellaneous styles */
  .padded-div {
    height: 20px;
  }
  .padding-top {
    padding-top: 10px;
  }
</style>
