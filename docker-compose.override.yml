version: "3.8"

services:
    ibf-api-service:
        # command: ["npm", "run", "start:dev"]
        command: ["npm", "run", "start"]

    ibf-web-interface:
        command: ["npm", "run", "start"]
        ports:
            - 4200:4200
